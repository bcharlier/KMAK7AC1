---
title: "TD 2 -- Analyse Factorielle des Correspondences et Analyse des Correspondances Multiples"
---

```{r}
#| label: setup
#| include: false
source("../../R/functions.R")
```

# Exercice 1. AFC et SVD généralisée

Il s'agit d'un tableau de contingence croisant 5 catégories de salariés (en lignes) et 4 catégories de fumeurs (en colonnes) au sein d'une entreprise :

- Lignes (Salariés) : `SM` : Senior Managers; `JM` : Junior Managers; `SE` : Senior Employees; `JE` : Junior Employees; `SC` : Secretaries.

```{r}
#| echo: true

library(ca)
data(smoke)
print_table(smoke)
sum(smoke)
```


0. On effectue un test préalable d'indépendance du $\chi^2$
```{r}
chisq.test(smoke, simulate.p.value=TRUE)
```

Que conclut-on quant à la significativité statistique de l'association ? Quel garde-fou méthodologique faut-il garder en tête lors des questions suivantes ?

<!-- Ce qu'il faut expliciter : Les proximités et alignements observés sur le biplot (ex. l'axe 1 opposant Heavy et None) ne constituent pas une preuve scientifique ou statistique.La formulation type : « Ces oppositions sont présentées à titre purement exploratoire et qualitatif. Étant donné le test du $\chi^2$ non significatif ($p = 0{,}165$), ces structures géométriques peuvent provenir d'un simple bruit d'échantillonnage. » -->



1. Préparation des données

  a) Donner le code R ou Python pour construire la matrice $F$ des fréquences, les vecteurs $r$ et $c$ des distributions marginales et la matrice $Z$ des écarts à l'indépendance.

```{r}
#| echo: false
  
# Matrice des fréquences
F <- smoke / sum(smoke)

# Distributions marginales (masses des lignes et colonnes)
r <- rowSums(F)
c <- colSums(F)

# Matrice Z des écarts à l'indépendance
Z <- (F - r %*% t(c)) / (r %*% t(c))
```

  b) Compléter les entrées manquantes `??` dans les sorties ci-dessous :

```{r}
#| echo: false

# Formatage pour affichage et masquage des valeurs
r_str <- as.character(round(r, 2))
r_str[2] <- " ??"

c_str <- as.character(round(c, 2))
c_str[3] <- " ??"

Z_str <- apply(round(Z, 2), 2, as.character)
dimnames(Z_str) <- dimnames(Z)
Z_str[3, 1] <- " ??"
```

Le vecteur $r$ des masses des lignes
```{r}
#| echo: false

print(r_str, quote = FALSE)
```

Le vecteur $c$ des masses des colonnes
```{r}
#| echo: false

print(c_str, quote = FALSE)
```

La matrice $Z$ des écarts à l'indépendance
```{r}
#| echo: false

print_table(Z_str)
```

---

2. Représentation des données sur les axes factoriels

  a) La fonction `gsvd` ci-dessous réalise la décomposition en valeurs singulières généralisée d’une matrice réelle $Z$ de dimension $n \times p$ avec les métriques diagonales $N = \operatorname{Diag}(r)$ sur $\mathbb{R}^n$ et $M = \operatorname{Diag}(c)$ sur $\mathbb{R}^p$.

```{r}
#| echo: true
#| code-fold: true

# Fonction SVD généralisée avec métriques diagonales
gsvd <- function(Z, r, c) {
  #-----entree---------------
  # Z matrice numerique de dimension (n,p) et de rang k
  # r poids de la metrique des lignes N=diag(r)
  # c poids de la metrique des colonnes M=diag(c)
  #-----sortie---------------
  # d vecteur de taille k contenant les valeurs singulieres (racines carres des valeurs propres)
  # U matrice de dimension (n,k) des vecteurs propres de de ZMZ'N
  # V matrice de dimension (p,k) des vecteurs propres de de Z'NZM
  #----------------------------

  k <- qr(Z)$rank
  colnames_Z <- colnames(Z)
  rownames_Z <- rownames(Z)
  Z <- as.matrix(Z)
  
  Ztilde <- diag(sqrt(r)) %*% Z %*% diag(sqrt(c))
  e <- svd(Ztilde)
  
  U <- diag(1 / sqrt(r)) %*% e$u[, 1:k]
  V <- diag(1 / sqrt(c)) %*% e$v[, 1:k]
  d <- e$d[1:k]
  
  rownames(U) <- rownames_Z
  rownames(V) <- colnames_Z
  if (length(d) > 1) {
    colnames(U) <- colnames(V) <- paste0("dim", 1:k)
  }
  return(list(U = U, V = V, d = d))
}
```

  Donner le code qui calcule, avec la fonction `gsvd` les quantités suivantes. Le vecteur $\Lambda$ des valeurs singulières

```{r}
#| echo: false

res_gsvd <- gsvd(Z, r, c)
U <- res_gsvd$U
V <- res_gsvd$V
Lambda <- res_gsvd$d

X <- sweep(U, 2, STATS = Lambda, FUN = "*")
Y <- sweep(V, 2, STATS = Lambda, FUN = "*")
```

```{r}
print_table(Lambda)
```

et les matrices $X$ et $Y$ des coordonnées factorielles des profils-lignes et profils-colonnes de l’AFC

::: {.columns}

::: {.column}

```{r}
print_table(X)
```

:::

::: {.column}

```{r}
print_table(Y)
```

:::

:::

  b) Calculer les inerties associées à chaque axe ainsi que le pourcentage d'inertie expliquée par le premier plan factoriel.

  c) Représenter à la main (ou sous R) les profils-lignes et les profils-colonnes sur le premier plan factoriel de l’AFC.

3. Exploitation des sorties `FactoMineR` suivantes :

```{r}
#| echo: true

library(FactoMineR)

res <- CA(smoke, graph = FALSE)

stopifnot(isTRUE(all.equal(abs(unname(res$row$coord)), abs(unname(X)), tolerance = 1e-8)))
stopifnot(isTRUE(all.equal(abs(unname(res$col$coord)), abs(unname(Y)), tolerance = 1e-8)))
```



  a) Expliquer le code R ci-dessus. 
  
  b) On donne:
  
::: {.columns}
  
::: {.column}
  
```{r}
print_table(res$row$contrib)
```
  
:::
  
::: {.column}
  
```{r}
print_table(res$col$contrib)
```
  
:::
  
:::
  
  Pourquoi ne prend on pas la peine d'afficher `res$row$cos2` et `res$col$cos2` ?
  
  c) Interpréter les axes factoriels et le graphique réalisé à la question 2.c.







# Exercice 2. 

On considère un jeu de données référençant $1\,044$ passagers du *Titanic*, décrivant les variables suivantes :

- **`Survived` :** Survie du passager ($0 = \text{Non}$, $1 = \text{Oui}$)
- **`Pclass` :** Classe du billet ($1^{\text{ère}}$, $2^{\text{ème}}$ ou $3^{\text{ème}}$)
- **`Sex` :** Sexe du passager
- **`Age` :** Tranche d'âge ($\left[0\,; 10\right]$, $\left[11\,; 18\right]$, $\left[19\,; 29\right]$, $\left[30\,; 49\right]$, $\ge 50$)
- **`Fare` :** Prix du ticket (variable quantitative supplémentaire)
- **`Embarked` :** Port d'embarquement ($\text{C} = \text{Cherbourg}$, $\text{Q} = \text{Queenstown}$, $\text{S} = \text{Southampton}$)

```{r}
#| echo: false


library(FactoMineR)
suppressPackageStartupMessages(library(PASWR))

data("titanic3")
df <- titanic3[, c("survived", "pclass", "sex", "age", "fare", "embarked")]

# -------------------------- #
#    Clean up data           #
# -------------------------- #

# Suppression des lignes ayant un NA sur la variable 'age'
df_clean <- df[!is.na(df$age), ]

# 2. Recodage de embarked pour ne garder que la première lettre ("C", "Q", "S")
df_clean$embarked <- factor(substr(df_clean$embarked, 1, 1))

# 2bis. Nettoyage des niveaux vides éventuels et vérification
df_clean <- df_clean[df_clean$embarked %in% c("C", "Q", "S"), ]
df_clean$embarked <- droplevels(df_clean$embarked)

# -------------------------- #
#    Convert variables       #
# -------------------------- #

# 3. Mise en forme des variables qualitatives
df_clean$Survived <- factor(df_clean$survived)
# table(df_clean$Survived)
df_clean$Pclass   <- factor(df_clean$pclass)
# table(df_clean$Pclass)
df_clean$Sex      <- factor(df_clean$sex)
# table(df_clean$Sex)
df_clean$Embarked <- factor(df_clean$embarked)
# table(df_clean$embarked)
df_clean$Fare <- df_clean$fare


# 2. Découpage de l'Age en tranches
df_clean$Age_cat <- cut(
  df_clean$age,
  breaks = c(0, 10, 18, 29, 49, Inf),
  labels = c("[0;10]", "[11;18]", "[19;29]", "[30;49]", ">=50"),
  include.lowest = TRUE
)

# 3. Sélection des variables (Fare est placée en dernière position)
df_titanic <- df_clean[, c("Survived", "Pclass", "Sex", "Age_cat", "Embarked", "Fare")]
#summary(df_titanic)
```

```{r}
#| echo: false


titanic.mca <- MCA(df_titanic, quanti.sup = 6, ncp=10, graph = FALSE)
```

1. On affiche d’abord les résultats suivants


::: {.columns}

::: {.column width="60%"}
```{r}
#| echo: false

print_table(titanic.mca$eig[,1:3])
```


:::

::: {.column width="40%"}

```{r}
#| echo: false


plot(titanic.mca$eig[,1], cex = 1.3, cex.axis = 1.3, cex.lab = 1.3, cex.main = 1.3)
```

:::

:::

  (a) Justifier le fait qu’il y ait 10 lignes dans ce tableau
  (b) À partir de ces affichages, combien de directions principales aurait-on envie de conserver ?

2. On trace les représentations ci-dessous


::: {.columns}

::: {.column}

```{r}
#| echo: false
#| fig-width: 8
#| fig-height: 8

plot(
  titanic.mca,
  axes = c(1, 2),
  choix = "ind",
  invisible = "quanti.sup",
  col.ind = "lightgrey",
  col.var = "red",
  col.quanti.sup = "blue",
  label = "var",
  cex = 1.3,
  cex.axis = 1.3,
  cex.lab = 1.3,
  cex.main = 1.3,
)

```


:::

::: {.column}
```{r}
#| echo: false
#| fig-width: 8
#| fig-height: 8


plot(
  titanic.mca,
    axes = c(1, 2),
  choix = "var",               # Affiche les catégories (modalités)
  col.var = "red",             # Modalités en rouge
  col.quanti.sup = "darkblue", # Variable Fare en bleu foncé
  cex = 1.3,
  cex.axis = 1.3,
  cex.lab = 1.3,
  cex.main = 1.3,
)
```

:::

:::



::: {.columns}

::: {.column}
```{r}
#| echo: false
#| fig-width: 8
#| fig-height: 8

plot(
  titanic.mca,
  axes = c(2, 3),
  choix = "ind",
  invisible = "none",
  col.ind = "lightgrey",
  col.var = "red",
  col.quanti.sup = "blue",
  label = "var",
  title = "ACM Titanic - Plan factoriel",
  cex = 1.3,
  cex.axis = 1.3,
  cex.lab = 1.3,
  cex.main = 1.3,
)

```


:::

::: {.column}



```{r}
#| echo: false
#| fig-width: 8
#| fig-height: 8

plot(
  titanic.mca,
  axes = c(2, 3),              # Axes 2 et 3
  choix = "var",               # Affiche les catégories (modalités)
  col.var = "red",             # Modalités en rouge
  col.quanti.sup = "darkblue", # Variable Fare en bleu foncé
  cex = 1.3,
  cex.axis = 1.3,
  cex.lab = 1.3,
  cex.main = 1.3,
)
```

:::

:::


  a) Commenter la représentation des individus dans ces plans.
  b) Rappeler comment sont calculées les positions des modalités sur ce graphe.
  c) Commenter la représentation des variables dans ces 2 plans.
  d) Interprétez les positions des différentes modalités par rapport à celles de la variable Survived.

3. On affiche à présent les sorties suivantes


::: {.columns}

::: {.column}

```{r}
print_table(
    titanic.mca$var$contrib[,1:3]
)
```

:::

::: {.column}

```{r}
print_table(
    titanic.mca$var$cos2[,1:3]
)
```

:::

:::
Quelle tendance semble se dégager ?

4. Synthétiser votre analyse en quelques lignes.
