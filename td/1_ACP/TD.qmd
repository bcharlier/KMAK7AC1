---
title: "TD 1 -- Analyse en Composantes Principales"
---

```{r}
#| label: setup
#| include: false
source("../../R/functions.R")
```

# Exercice 1. Un peu de calcul matriciel

On considère la matrice

$$
X =
\begin{pmatrix}
1 & 0 & 0 \\
1 & 2 & 0 \\
2 & 2 & 2 \\
0 & 0 & 2
\end{pmatrix}
$$

1. Caluler $Z$, la version centrée-réduite de la matrice $X$.

2. En déduire la matrice d'inertie de $Z$ $S$ associée à $X$.

3. Diagonaliser la matrice $S$ en l'écrivant sous la forme

$$
S = P \Lambda P^{-1},
$$

où $\Lambda$ est une matrice diagonale de termes diagonaux
$\{\lambda_i\}_{i=1,2,3}$.

4. On rappelle que la trace d'une matrice carrée $A$ de taille $n \times n$
correspond à la somme de ses termes diagonaux :

$$
\operatorname{tr}(A) = \sum_{i=1}^n A_{i,i}.
$$

Montrer que si $U$ est une matrice de taille $n \times m$ et $V$ une
matrice de taille $m \times n$, alors

$$
\operatorname{tr}(UV) = \operatorname{tr}(VU).
$$

En déduire que

$$
\operatorname{tr}(S) = \sum_{i=1}^3 \lambda_i = p.
$$

5. Calculer les parts d'inertie. Quelle(s) direction(s) aurait-on envie de
retenir si la matrice $X$ correspondait à un tableau de données ?

6. Calculer les composantes principales, et représenter les individus dans
le plan des deux premières composantes.

7. Déterminer les corrélations entre les variables et les composantes et les
représenter sur le cercle des corrélations dans le plan des deux premières
composantes.


# Exercice 2: Analyse sensorielle

On dispose de 10 mesures effectuées sur 10 cidres différents, afin d'évaluer
leur qualité olfacto-gustative.

```{r}
#| echo: false
cidre <- read.csv("data/cidre.csv", header = TRUE, sep = ";")

rownames(cidre) <- cidre$nom
cidre$nom <- NULL

cidre |>
  print_table()
```


1. Quel est l'objectif d'une ACP dans ce contexte ?

2. Les donnée sont dans un `data.frame` nommé `cidre` et on effectue
l'ACP dans R.

```{r}
library(FactoMineR)
cidre.acp = PCA(cidre, ncp = 10, scale.unit = TRUE, graph = FALSE)
```


:::{.panel-tabset}

### Tableau
```{r}
#| echo: false


cidre.acp$eig |>
  print_table()
```

### Graphique

```{r}
#| echo: false
plot(cidre.acp$eig[,1])
```


:::

Quelle est la part d’inertie expliquée par les deux premières composantes principales ? Combien de dimensions
aurait-on envie de retenir ?

3. On affiche les deux sorties R ci-dessous


::: {.columns}

::: {.column}

```{r}
cidre.acp$var$contrib[, 1:3] |>
  print_table()
```

:::

::: {.column}

```{r}
cidre.acp$var$cos2[, 1:3] |>
  print_table()
```

:::

:::

ainsi que les deux représentations graphiques suivantes.

::: {.panel-tabset}

### Dim 1-2
```{r}
#| echo: false
#| out-width: "100%"

plot(
  cidre.acp,
  choix = "var",
  axes = c(1, 2),
)
```

### Dim 2-3
```{r}
#| echo: false
#| out-width: "100%"

plot(
  cidre.acp,
  choix = "var",
  axes = c(2, 3),
)
```

### Interactif 3D
```{r}
#| echo: false
#| out-width: "100%"

suppressPackageStartupMessages(library(plotly))

variables <- as.data.frame(cidre.acp$var$coord[, 1:3])
variables$nom <- rownames(variables)

theta <- seq(0, 2 * pi, length.out = 80)
phi <- seq(0, pi, length.out = 40)
sphere <- expand.grid(theta = theta, phi = phi)
sphere$x <- cos(sphere$theta) * sin(sphere$phi)
sphere$y <- sin(sphere$theta) * sin(sphere$phi)
sphere$z <- cos(sphere$phi)

segments <- data.frame(
  x = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.1, NA)),
  y = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.2, NA)),
  z = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.3, NA))
)

plot_ly() |>
  add_surface(
    x = matrix(sphere$x, nrow = length(theta), ncol = length(phi)),
    y = matrix(sphere$y, nrow = length(theta), ncol = length(phi)),
    z = matrix(sphere$z, nrow = length(theta), ncol = length(phi)),
    opacity = 0.15,
    showscale = FALSE,
    colorscale = list(c(0, "lightgray"), c(1, "lightgray")),
    name = "Sphère unité",
    hoverinfo = "none"
  ) |>
  add_trace(
    data = segments,
    x = ~x,
    y = ~y,
    z = ~z,
    type = "scatter3d",
    mode = "lines",
    line = list(color = "#2e74b5", width = 3),
    showlegend = FALSE,
    hoverinfo = "none"
  ) |>
  add_trace(
    data = variables,
    x = ~Dim.1,
    y = ~Dim.2,
    z = ~Dim.3,
    type = "scatter3d",
    mode = "markers+text",
    text = ~nom,
    textposition = "top center",
    marker = list(size = 4, color = "#d95f02"),
    hovertemplate = paste(
      "%{text}<br>",
      "Axe 1: %{x:.2f}<br>",
      "Axe 2: %{y:.2f}<br>",
      "Axe 3: %{z:.2f}<extra></extra>"
    ),
    name = "Variables"
  ) |>
  layout(
    scene = list(
      xaxis = list(title = "Axe 1", range = c(-1.1, 1.1)),
      yaxis = list(title = "Axe 2", range = c(-1.1, 1.1)),
      zaxis = list(title = "Axe 3", range = c(-1.1, 1.1)),
      camera = list(projection = list(type = "orthographic")),
      aspectmode = "cube"
    ),
    updatemenus = list(
      list(
        type = "buttons",
        direction = "right",
        x = 0.05,
        y = 1.12,
        buttons = list(
          list(
            label = "XY",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 0, z = 2.5),
                up = list(x = 0, y = 1, z = 0),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "YZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 2.5, y = 0, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "XZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 2.5, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          )
        )
      )
    ),
    margin = list(l = 55, r = 20, b = 55, t = 20)
  )
```

:::


  a. Quels seraient les variables mal représentées dans le premier plan principal ? Pourquoi ?
  
  b. Quels seraient les variables les plus corrélés (positivement ou négativement) au caractère alcool ? Celles qui le seraient le moins ?

  c. Quels seraient les caractères qui contribuent le plus au premier axe principal ?
  
  d. Même questions avec les deux autres axes.

4. On considère maintenant les sorties R

::: {.columns}

::: {.column}

```{r}
cidre.acp$ind$contrib[, 1:3] |>
  print_table()
```

:::

::: {.column}

```{r}
cidre.acp$ind$cos2[, 1:3] |>
  print_table()
```

:::

:::


ainsi que les représentations graphiques suivantes.

::: {.panel-tabset}

### Dim 1-2
```{r}
#| echo: false
#| out-width: "100%"

plot(
  cidre.acp,
  choix = "ind",
  axes = c(1, 2),
)
```

### Dim 2-3
```{r}
#| echo: false
#| out-width: "100%"

plot(
  cidre.acp,
  choix = "ind",
  axes = c(2, 3),
)
```

### Interactif 3D
```{r}
#| echo: false
#| out-width: "100%"

suppressPackageStartupMessages(library(plotly))

individus <- as.data.frame(cidre.acp$ind$coord[, 1:3])
individus$nom <- rownames(individus)

plot_ly(
  data = individus,
  x = ~Dim.1,
  y = ~Dim.2,
  z = ~Dim.3,
  type = "scatter3d",
  mode = "markers+text",
  text = ~nom,
  textposition = "top center",
  marker = list(size = 7, color = "#2e74b5"),
  hovertemplate = paste(
    "%{text}<br>",
    "Axe 1: %{x:.2f}<br>",
    "Axe 2: %{y:.2f}<br>",
    "Axe 3: %{z:.2f}<extra></extra>"
  ),
  name = "Individus"
) |>
  layout(
    scene = list(
      xaxis = list(title = "Axe 1"),
      yaxis = list(title = "Axe 2"),
      zaxis = list(title = "Axe 3"),
      camera = list(projection = list(type = "orthographic")),
      aspectmode = "cube"
    ),
    updatemenus = list(
      list(
        type = "buttons",
        direction = "right",
        x = 0.05,
        y = 1.12,
        buttons = list(
          list(
            label = "XY",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 0, z = 2.5),
                up = list(x = 0, y = 1, z = 0),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "YZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 2.5, y = 0, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "XZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 2.5, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          )
        )
      )
    ),
    margin = list(l = 55, r = 20, b = 55, t = 20)
  )
```

:::
  
  a. Les individus sont ils bien représentés ?
  
  b. Quels individus seraient représentatifs de profils opposés par rapport au premier axe principal ? Même question pour les deux autres axes.
  
  c. Proposer des profils de cidres, en précisant leurs caractéristiques principales.

## Exercice 3

On va considérer le jeu de données `decathlon` disponible dans le paquet `FactoMineR`, qui s’intéresse en particulier aux performances d’athlètes pour les 10 épreuves du décathlon, dans deux événements différents : les Jeux Olympiques de 2004 et le Décastar de 2004. On ne va considérer ici que cette dernière épreuve, dont on stockera les mesures
dans un `data.frame` nommé decastar

```{r}
require(FactoMineR)

# Charge dataset in R
data(decathlon) 

# keep only decastar competition
decastar = decathlon[decathlon$Competition == 'Decastar', ]
# Drop useless competition column
decastar = decastar[ , colnames(decastar) != 'Competition']
decastar |>
  print_table()
```

1. Quel est l’intérêt d’effectuer une ACP ici ? Sur quelles variables ?
2. Quelle opération est indispensable ici avant d’effectuer l’ACP ? Pourquoi ?
3. On affiche ci-dessous les résultats de l’ACP en termes de valeurs propres. Combien de directions aurait-on
envie de conserver ?

```{r}
deca.acp = PCA(decastar[, 1:11], quanti.sup= 11 )
```

:::{.panel-tabset}

### Tableau

```{r}
#| echo: false


deca.acp$eig |>
  print_table()
```


### Valeur propres

```{r}
#| echo: false
plot(deca.acp$eig[,1])
```

### Inertie cumulée

```{r}
#| echo: false
plot(deca.acp$eig[,3])
```




:::


4. Commenter les résultats numériques et graphiques ci-dessous en terme d’individus et de variables (suivre le
modèle de l’exercice précédent)


::: {.columns}

::: {.column}

```{r}
deca.acp$ind$contrib[, 1:4] |>
  print_table()
```

:::

::: {.column}


```{r}
deca.acp$ind$cos2[, 1:4] |>
  print_table()
```
:::

:::

::: {.panel-tabset}

### Dim 1-2

```{r}
#| echo: false
#| out-width: "100%"


plot(
  deca.acp,
  choix = "ind",
  axes = c(1, 2),
  invisible = "quanti.sup",
)
```


### Dim 1-2 (QLT)

```{r}
#| echo: false
#| out-width: "100%"

ind_dim12 <- data.frame(
  Dim.1 = deca.acp$ind$coord[, 1],
  Dim.2 = deca.acp$ind$coord[, 2],
  nom = rownames(deca.acp$ind$coord),
  cos2 = rowSums(deca.acp$ind$cos2[, 1:2])
)

ggplot2::ggplot(ind_dim12, ggplot2::aes(Dim.1, Dim.2, color = cos2)) +
  ggplot2::geom_hline(yintercept = 0, color = "gray80") +
  ggplot2::geom_vline(xintercept = 0, color = "gray80") +
  ggplot2::geom_point(size = 3) +
  ggplot2::geom_text(ggplot2::aes(label = nom), vjust = -0.8, show.legend = FALSE) +
  ggplot2::scale_color_gradient2(
    low = "#d73027",
    mid = "#fee08b",
    high = "#1a9850",
    midpoint = 0.5,
    limits = c(0, 1),
    name = "cos2 cumulé"
  ) +
  ggplot2::labs(
    title = "Individus - cos2 cumulé sur les dimensions 1-2",
    x = "Dimension 1",
    y = "Dimension 2"
  ) +
  ggplot2::coord_equal() +
  ggplot2::theme_minimal()
```


### Dim 3-4

```{r}
#| echo: false
#| out-width: "100%"


plot(
  deca.acp,
  choix = "ind",
  axes = c(3, 4),
  invisible = "quanti.sup",
)
```


### Dim 3-4 (QLT)

```{r}
#| echo: false
#| out-width: "100%"

ind_dim12 <- data.frame(
  Dim.3 = deca.acp$ind$coord[, 3],
  Dim.4 = deca.acp$ind$coord[, 4],
  nom = rownames(deca.acp$ind$coord),
  cos2 = rowSums(deca.acp$ind$cos2[, 3:4])
)

ggplot2::ggplot(ind_dim12, ggplot2::aes(Dim.3, Dim.4, color = cos2)) +
  ggplot2::geom_hline(yintercept = 0, color = "gray80") +
  ggplot2::geom_vline(xintercept = 0, color = "gray80") +
  ggplot2::geom_point(size = 3) +
  ggplot2::geom_text(ggplot2::aes(label = nom), vjust = -0.8, show.legend = FALSE) +
  ggplot2::scale_color_gradient2(
    low = "#d73027",
    mid = "#fee08b",
    high = "#1a9850",
    midpoint = 0.5,
    limits = c(0, 1),
    name = "cos2 cumulé"
  ) +
  ggplot2::labs(
    title = "Individus - cos2 cumulé sur les dimensions 3-4",
    x = "Dimension 3",
    y = "Dimension 4"
  ) +
  ggplot2::coord_equal() +
  ggplot2::theme_minimal()
```



:::




::: {.columns}

::: {.column}

```{r}
deca.acp$var$contrib[, 1:4] |>
  print_table()
```

:::

::: {.column}

```{r}
deca.acp$var$cos2[, 1:4] |>
  print_table()
```

:::

:::




::: {.panel-tabset}

### Dim 1-2


```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(1, 2),
  invisible = "quanti.sup",
)
```


### Dim 3-4

```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(3, 4),
  invisible = "quanti.sup",
)
```


### Interactive plot

```{r}
#| echo: false
#| out-width: "100%"

suppressPackageStartupMessages(library(plotly))

variables <- as.data.frame(deca.acp$var$coord[, 1:3])
variables$nom <- rownames(variables)

segments <- data.frame(
  x = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.1, NA)),
  y = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.2, NA)),
  z = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.3, NA))
)

plot_ly() |>
  add_trace(
    data = segments,
    x = ~x,
    y = ~y,
    z = ~z,
    type = "scatter3d",
    mode = "lines",
    line = list(color = "#2e74b5", width = 3),
    showlegend = FALSE,
    hoverinfo = "none"
  ) |>
  add_trace(
    data = variables,
    x = ~Dim.1,
    y = ~Dim.2,
    z = ~Dim.3,
    type = "scatter3d",
    mode = "markers+text",
    text = ~nom,
    textposition = "top center",
    marker = list(size = 4, color = "#d95f02"),
    hovertemplate = paste(
      "%{text}<br>",
      "Axe 1: %{x:.2f}<br>",
      "Axe 2: %{y:.2f}<br>",
      "Axe 3: %{z:.2f}<extra></extra>"
    ),
    name = "Variables"
  ) |>
  layout(
    scene = list(
      xaxis = list(title = "Axe 1"),
      yaxis = list(title = "Axe 2"),
      zaxis = list(title = "Axe 3"),
      camera = list(projection = list(type = "orthographic")),
      aspectmode = "cube"
    ),
    updatemenus = list(
      list(
        type = "buttons",
        direction = "right",
        x = 0.05,
        y = 1.12,
        buttons = list(
          list(
            label = "XY",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 0, z = 2.5),
                up = list(x = 0, y = 1, z = 0),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "YZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 2.5, y = 0, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "XZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 2.5, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          )
        )
      )
    ),
    margin = list(l = 55, r = 20, b = 55, t = 20)
  )
```


:::

5. On se propose à présent de mettre en parallèle le classement des athlètes avec les résultats obtenus. Commenter les graphiques ci-dessous.


::: {.panel-tabset}


### Dim 1-2


```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(1, 2),
)
```


### Dim 3-4

```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(3, 4),
)
```

### Interactive plot

```{r}
#| echo: false
#| out-width: "100%"

suppressPackageStartupMessages(library(plotly))

variables <- as.data.frame(deca.acp$var$coord[, 1:3])
variables$nom <- rownames(variables)

rang <- data.frame(
  Dim.1 = deca.acp$quanti.sup$coord["Rank", "Dim.1"],
  Dim.2 = deca.acp$quanti.sup$coord["Rank", "Dim.2"],
  Dim.3 = deca.acp$quanti.sup$coord["Rank", "Dim.3"],
  nom = "Rang"
)

rang_segment <- data.frame(
  x = c(0, rang$Dim.1, NA),
  y = c(0, rang$Dim.2, NA),
  z = c(0, rang$Dim.3, NA)
)

segments <- data.frame(
  x = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.1, NA)),
  y = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.2, NA)),
  z = as.vector(rbind(rep(0, nrow(variables)), variables$Dim.3, NA))
)

plot_ly() |>
  add_trace(
    data = segments,
    x = ~x,
    y = ~y,
    z = ~z,
    type = "scatter3d",
    mode = "lines",
    line = list(color = "#2e74b5", width = 3),
    showlegend = FALSE,
    hoverinfo = "none"
  ) |>
  add_trace(
    data = variables,
    x = ~Dim.1,
    y = ~Dim.2,
    z = ~Dim.3,
    type = "scatter3d",
    mode = "markers+text",
    text = ~nom,
    textposition = "top center",
    marker = list(size = 4, color = "#d95f02"),
    hovertemplate = paste(
      "%{text}<br>",
      "Axe 1: %{x:.2f}<br>",
      "Axe 2: %{y:.2f}<br>",
      "Axe 3: %{z:.2f}<extra></extra>"
    ),
    name = "Variables"
  ) |>
  add_trace(
    data = rang_segment,
    x = ~x,
    y = ~y,
    z = ~z,
    type = "scatter3d",
    mode = "lines",
    line = list(color = "#1b9e77", width = 5),
    showlegend = FALSE,
    hoverinfo = "none"
  ) |>
  add_trace(
    data = rang,
    x = ~Dim.1,
    y = ~Dim.2,
    z = ~Dim.3,
    type = "scatter3d",
    mode = "lines+markers+text",
    text = ~nom,
    textposition = "top center",
    line = list(color = "#1b9e77", width = 5),
    marker = list(size = 4, color = "#1b9e77"),
    hovertemplate = paste(
      "%{text}<br>",
      "Axe 1: %{x:.2f}<br>",
      "Axe 2: %{y:.2f}<br>",
      "Axe 3: %{z:.2f}<extra></extra>"
    ),
    name = "Variable supplémentaire"
  ) |>
  layout(
    scene = list(
      xaxis = list(title = "Axe 1"),
      yaxis = list(title = "Axe 2"),
      zaxis = list(title = "Axe 3"),
      camera = list(projection = list(type = "orthographic")),
      aspectmode = "cube"
    ),
    updatemenus = list(
      list(
        type = "buttons",
        direction = "right",
        x = 0.05,
        y = 1.12,
        buttons = list(
          list(
            label = "XY",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 0, z = 2.5),
                up = list(x = 0, y = 1, z = 0),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "YZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 2.5, y = 0, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          ),
          list(
            label = "XZ",
            method = "relayout",
            args = list(list(
              scene.camera = list(
                eye = list(x = 0, y = 2.5, z = 0),
                up = list(x = 0, y = 0, z = 1),
                projection = list(type = "orthographic")
              )
            ))
          )
        )
      )
    ),
    margin = list(l = 55, r = 20, b = 55, t = 20)
  )
```

:::

