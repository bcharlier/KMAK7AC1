---
title: "TD 1 -- Analyse en Composantes Principales"
---

```{r}
#| label: setup
#| include: false
source("../../R/functions.R")
```

# Exercice 1. Un peu de calcul matriciel

On considère la matrice

$$
X =
\begin{pmatrix}
1 & 0 & 0 \\
1 & 2 & 0 \\
2 & 2 & 2 \\
0 & 0 & 2
\end{pmatrix}
$$

1. Cette matrice est-elle centrée-réduite ? Appliquer cette transformation si ce n'est pas le cas.

2. En déduire la matrice de corrélation $S$ associée.

3. Diagonaliser la matrice $S$ en l'écrivant sous la forme

$$
S = P \Lambda P^{-1},
$$

où $\Lambda$ est une matrice diagonale de termes diagonaux
$\{\lambda_i\}_{i=1,2,3}$.

4. On rappelle que la trace d'une matrice carrée $A$ de taille $n \times n$
correspond à la somme de ses termes diagonaux :

$$
\operatorname{tr}(A) = \sum_{i=1}^n A_{i,i}.
$$

Montrer que si $U$ est une matrice de taille $n \times m$ et $V$ une
matrice de taille $m \times n$, alors

$$
\operatorname{tr}(UV) = \operatorname{tr}(VU).
$$

En déduire que

$$
\operatorname{tr}(S) = \sum_{i=1}^3 \lambda_i = p.
$$

5. Calculer les parts d'inertie. Quelle(s) direction(s) aurait-on envie de
retenir si la matrice $X$ correspondait à un tableau de données ?

6. Calculer les composantes principales, et représenter les individus dans
le plan des deux premières composantes.

7. Déterminer les corrélations entre les variables et les composantes et les
représenter sur le cercle des corrélations dans le plan des deux premières
composantes.


# Exercice 2: Analyse sensorielle

On dispose de 10 mesures effectuées sur 10 cidres différents, afin d'évaluer
leur qualité olfacto-gustative.

```{r}
#| echo: false
cidre <- read.csv("data/cidre.csv", header = TRUE, sep = ";")
cidre |>
  print_table()
```


1. Quel est l'objectif d'une ACP dans ce contexte ?

2. Les donnée sont dans un `data.frame` nommé `cidre` et on effectue
l'ACP dans R.

```{r}
library(FactoMineR)
cidre.acp = PCA(cidre[,-1], ncp=5, scale.unit = TRUE, graph = FALSE)
cidre.acp$eig |>
  print_table()
```

Quelle est la part d’inertie expliquée par les deux premières composantes principales ? Combien de dimensions
aurait-on envie de retenir ?

3. On affiche les deux sorties R ci-dessous


::: {.columns}

::: {.column}

```{r}
cidre.acp$var$contrib[, 1:3] |>
  print_table()
```

:::

::: {.column}

```{r}
cidre.acp$var$cos2[, 1:3] |>
  print_table()
```

:::

:::

ainsi que les deux représentations graphiques suivantes.

::: {.columns}

::: {.column width="50%"}
```{r}
#| echo: false
#| out-width: "100%"

plot(
  cidre.acp,
  choix = "var",
  axes = c(1, 2),
)
```
:::

::: {.column width="50%"}
```{r}
#| echo: false
#| out-width: "100%"

plot(
  cidre.acp,
  choix = "var",
  axes = c(2, 3),
)
```
:::

:::


  a. Quels seraient les caractères mal représentés dans le premier plan principal ? Pourquoi ?
  b. Quels seraient les caractères les plus corrélés (positivement ou négativement) au caractère alcool ? Celles
    qui le seraient le moins ?

4. On considère maintenant les sorties R

::: {.columns}

::: {.column}

```{r}
cidre.acp$ind$contrib[, 1:3] |>
  print_table()
```

:::

::: {.column}

```{r}
cidre.acp$ind$cos2[, 1:3] |>
  print_table()
```

:::

:::


ainsi que les deux représentations graphiques suivantes.

::: {.columns}

::: {.column width="50%"}
```{r}
#| echo: false
#| out-width: "100%"


plot(
  cidre.acp,
  choix = "ind",
  axes = c(1, 2),
)
```
:::

::: {.column width="50%"}
```{r}
#| echo: false
#| out-width: "100%"

plot(
  cidre.acp,
  choix = "ind",
  axes = c(2, 3),
)
```
:::

:::


## Exercice 3

On va considérer le jeu de données `decathlon` disponible dans le paquet `FactoMineR`, qui s’intéresse en particulier aux performances d’athlètes pour les 10 épreuves du décathlon, dans deux événements différents : les Jeux Olympiques de 2004 et le Décastar de 2004. On ne va considérer ici que cette dernière épreuve, dont on stockera les mesures
dans un `data.frame` nommé decastar

```{r}
require(FactoMineR)

# Charge dataset in R
data(decathlon) 

# keep only decastar competition
decastar = decathlon[decathlon$Competition == 'Decastar', ]
# Drop useless competition column
decastar = decastar[ , colnames(decastar) != 'Competition']
decastar |>
  print_table()
```

1. Quel est l’intérêt d’effectuer une ACP ici ? Sur quelles variables ?
2. Quelle opération est indispensable ici avant d’effectuer l’ACP ? Pourquoi ?
3. On affiche ci-dessous les résultats de l’ACP en termes de valeurs propres. Combien de directions aurait-on
envie de conserver ?

```{r}
deca.acp = PCA(decastar[, 1:11], quanti.sup= 11 )
deca.acp$eig |>
  print_table()
```

4. Commenter les résultats numériques et graphiques ci-dessous en terme d’individus et de variables (suivre le
modèle de l’exercice précédent)


::: {.columns}

::: {.column}

```{r}
deca.acp$ind$contrib[, 1:4] |>
  print_table()
```

:::

::: {.column}

```{r}
deca.acp$var$contrib[, 1:4] |>
  print_table()
```

:::

:::



::: {.columns}

::: {.column}

```{r}
deca.acp$ind$cos2[, 1:4] |>
  print_table()
```

:::

::: {.column}

```{r}
deca.acp$var$cos2[, 1:4] |>
  print_table()
```

:::

:::

::: {.columns}

::: {.column}

```{r}
#| echo: false
#| out-width: "100%"


plot(
  deca.acp,
  choix = "ind",
  axes = c(1, 2),
  invisible = "quanti.sup",
)
```

:::

::: {.column}

```{r}
#| echo: false
#| out-width: "100%"


plot(
  deca.acp,
  choix = "ind",
  axes = c(3, 4),
  invisible = "quanti.sup",
)
```

:::

:::


::: {.columns}

::: {.column}

```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(1, 2),
  invisible = "quanti.sup",
)
```

:::

::: {.column}

```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(3, 4),
  invisible = "quanti.sup",
)
```

:::

:::

5. On se propose à présent de mettre en parallèle le classement des athlètes avec les résultats obtenus. Commenter les graphiques ci-dessous.


::: {.columns}

::: {.column}

```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(1, 2),
)
```

:::

::: {.column}

```{r}
#| echo: false
#| out-width: "100%"

plot(
  deca.acp,
  choix = "var",
  axes = c(3, 4),
)
```

:::

:::